#include <bits/stdc++.h>
using namespace std;
int main()
{
    int arr[10];
    cout << arr[1];

    cin >> arr[1];

    cout << arr[1];

    string s = "Hello";

    cout << "\n"
         << s[4];

    cout << s.size();
    cout << s[s.size() - 1]; // last element

    cout << s.length(); // size and length method are use to get length of the string

    //  So both arrays and strings works same!
    return 0;
}