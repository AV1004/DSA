#include <bits/stdc++.h>
using namespace std;
int main()
{
    int age;
    cin >> age;
    if (age >= 18)
    {

        cout << "You are eligible for DL!";
    }
    else
    {
        cout << "You are not eligible for DL!";
    }

    return 0;
}

// The other condtional statements like if,else if,ladder if else etc works same in c++